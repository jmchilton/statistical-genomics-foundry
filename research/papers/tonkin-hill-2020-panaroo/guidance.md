# Guidance — tonkin-hill-2020-panaroo (paper)

> Attention-directing. THE validity-axis primary for gene-family-evolution: annotation
> heterogeneity / errors inflate apparent gene-family size differences. CC BY 4.0 (Genome
> Biology) — short verbatim load-bearing quotes ARE allowed (with location + attribution).

## Must capture
- The core mechanism: how inconsistent/erroneous gene annotation across genomes inflates the
  accessory genome / apparent gene-family-size differences. Capture the named error sources the
  paper enumerates (fragmented genes, mis-assembly, contamination, annotation-pipeline differences)
  and which ones drive spurious size variation.
- The exact organism / data SCOPE. This is load-bearing: the paper is about **prokaryotic /
  bacterial pangenomes**. Does the paper anywhere claim or test that the same annotation-error
  inflation applies to EUKARYOTES, or to CAFE-style birth-death models? Quote any generalization the
  paper actually makes; if it does NOT generalize beyond bacteria, say so explicitly. (The skill
  asserts "same principle in eukaryotes" and applies it to CAFE5 — we need to know if that is the
  paper's claim or the skill's inference.)
- Any quantitative error-rate results: the simulated-dataset comparisons (accessory-inflation counts,
  missing-gene / false-negative rates) and the conditions (contamination level, assembly
  fragmentation) under which inflation is worst. Capture the paper's own numbers.
- Whether the paper frames annotation consistency as a PREREQUISITE for downstream evolutionary
  inference (gene gain/loss, family dynamics). Quote any such statement.
- Panaroo's remedy at a conceptual level (pangenome graph correcting annotation errors) — enough to
  recover what "consistent annotation" operationally means, without the full tool workflow.

## Must-quote (CC BY → short verbatim allowed, with section/figure location)
- The sentence(s) stating that annotation errors produce spurious pangenome / gene-family-size
  inflation.
- Any explicit scope boundary or generalization statement (bacteria-only vs. broader).

## Access / version notes
- Genome Biology 21:180, 2020; DOI 10.1186/s13059-020-02090-4. Fully OA, CC BY 4.0.
- Do NOT let the skill's eukaryote-generalization bend the summary — report only the paper's scope.
