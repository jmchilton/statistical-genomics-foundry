---
type: book
title: Generative Models for Discrete Data
source: msmb
source_chapter: 1
source_url: https://www.huber.embl.de/msmb/01-chap.html
# --- book.yml (generated by `npm run books`) — edit book.yml, not here ---
license: CC-BY-NC-SA-2.0
license_file: LICENSES/msmb.LICENSE
attribution: "Holmes S, Huber W. Modern Statistics for Modern Biology. Cambridge University Press; 2019. https://www.huber.embl.de/msmb/"
derived: own-words-summary
# --- end book.yml ---
tags:
  - domain/statistical-inference
---

# Generative Models for Discrete Data

The first six fields are the chapter's own. The block between the markers is the licence
record every chapter of this book shares, authored once in
`content/research/books/msmb/book.yml` and copied here by `npm run books`.

It is COPIED rather than merged in while validating, so a chapter note validates from its own
frontmatter like every other note in the corpus: no file reads inside a zod schema, and the
kind manifest lists what a book chapter actually carries. `npm run check:books` fails CI if a
copy drifts from `book.yml`, which is what keeps the copy from becoming a second source of
truth — the same trade `kinds.generated.json` makes.

Edit `book.yml` and regenerate; editing the block here is overwritten on the next run.
